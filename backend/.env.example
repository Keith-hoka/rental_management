# Copy to .env and fill in real values. .env is gitignored.
# Without RESEND_API_KEY, password reset emails are logged instead of sent.

RESEND_API_KEY=
EMAIL_FROM=onboarding@resend.dev
FRONTEND_URL=http://localhost:3000
